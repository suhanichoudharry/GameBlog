import { ESLintUtils } from "@typescript-eslint/utils";
export declare const CONVEX_REGISTRARS: string[];
/**
 * Assuming this is only called on files in the convex directory,
 * check return true if the file looks like an entry point.
 * This logic matches convex/src/bundler/index.ts.
 */
export declare function isEntryPoint(fpath: string): boolean;
export declare const createRule: <Options extends readonly unknown[], MessageIds extends string>({ meta, name, ...rule }: Readonly<ESLintUtils.RuleWithMetaAndName<Options, MessageIds, unknown>>) => ESLintUtils.RuleModule<MessageIds, Options, unknown, ESLintUtils.RuleListener>;
//# sourceMappingURL=util.d.ts.map