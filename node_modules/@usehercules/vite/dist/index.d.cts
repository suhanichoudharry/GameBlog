import { Plugin } from "vite";

//#region src/component-tagger.d.ts
interface ComponentTaggerOptions {
  enabled?: boolean;
  debug?: boolean;
  /**
   * The data attribute to use for element identification
   * @default "data-component-id"
   */
  dataAttribute?: string;
}
//#endregion
//#region src/visual-editor/index.d.ts
interface VisualEditorOptions {
  /**
   * Enable debug logging
   * @default false
   */
  debug?: boolean;
  /**
   * The data attribute to use for element identification
   * @default "data-hercules-id"
   */
  dataAttribute?: string;
}
//#endregion
//#region src/dynamic-component-creator.d.ts
interface DynamicComponentCreatorOptions {
  /**
   * Enable debug logging
   * @default false
   */
  debug?: boolean;
  /**
   * Force enable the plugin even when HERCULES_DEV_MACHINE is not set
   * @default false
   */
  force?: boolean;
  /**
   * Base path for resolving @/ imports
   * @default 'src'
   */
  aliasBase?: string;
}
/**
 * Vite plugin that dynamically creates missing React component files
 * when they are imported but don't exist yet.
 */
declare function dynamicComponentCreatorPlugin(options?: DynamicComponentCreatorOptions): Plugin;
//#endregion
//#region src/index.d.ts
interface HerculesPluginOptions {
  /**
   * Enable debug logging
   * @default true
   */
  debug?: boolean;
  /**
   * Custom message to log during build
   * @default 'Hercules plugin is running!'
   */
  message?: string;
  /**
   * Enable Vite error handling and console logging
   * @default true
   */
  handleViteErrors?: boolean;
  /**
   * Component tagger options
   * @default { enabled: true }
   */
  componentTagger?: ComponentTaggerOptions & {
    enabled?: boolean;
  };
  /**
   * Visual editor options
   * @default { enabled: true }
   */
  visualEditor?: VisualEditorOptions & {
    enabled?: boolean;
  };
  /**
   * Dynamic component creator options
   * Note: Only active when HERCULES_DEV_MACHINE environment variable is set,
   * unless 'force' option is true
   * @default { enabled: true }
   */
  dynamicComponentCreator?: DynamicComponentCreatorOptions & {
    enabled?: boolean;
  };
}
/**
 * Hercules Vite plugin for development workspace integration
 * Handles error reporting and console forwarding for the Hercules platform
 */
declare function hercules(options?: HerculesPluginOptions): Plugin[];
//#endregion
export { type DynamicComponentCreatorOptions, HerculesPluginOptions, hercules as default, hercules, dynamicComponentCreatorPlugin };
//# sourceMappingURL=index.d.cts.map